{
  "name": "@undercutsh/cli",
  "version": "0.1.0",
  "description": "Undercut Pro CLI: pair a machine, sync the signed policy artifact, check status, and log out. Most of this package is a structural scaffold — see README.md for what actually talks to a server today (nothing does yet).",
  "type": "module",
  "private": true,
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "bin": {
    "undercutsh": "./bin/undercutsh.js"
  },
  "files": [
    "bin",
    "src"
  ],
  "scripts": {
    "start": "node bin/undercutsh.js",
    "lint": "node --check bin/undercutsh.js && for f in src/**/*.js; do node --check \"$f\"; done",
    "test": "node --test 'src/**/*.test.js'"
  },
  "dependencies": {
    "commander": "^12.1.0"
  },
  "keywords": [
    "undercut",
    "cli",
    "tiered-dispatch"
  ]
}
